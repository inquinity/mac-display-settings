import FinderSync

// The host app exists only to carry the Finder Sync extension in its
// PlugIns folder. Launching it opens the extension settings so the user
// can turn the extension on, then quits.
FIFinderSyncController.showExtensionManagementInterface()
